var scrap=require('scrap');

exports.scrapAllJobs = function(req,res){
  var _arrayOfJobs = [];
}

var _getPagination = function(url,callback){

}
var _scrapurl = function(url,callback){
  scrap(url,function(err,$){
    if(err)console.log(err);return;
    $('.jobs-list').find('li').each(function(i,row){
  //console.log(row);
/*  var json = { job_title : "", company : "", localization : "",  category : "",  description : "", contract : "", date: "", tags : []};
  json.job_title = $(this).find('.job-title').text().trim();
  json.company = $(this).find('.company').text();
  json.localization = $(this).find('.workplace').text();
  json.category = $(this).find('.job-title .job-link').attr('href').split('/')[2];
  //json.description = $(this).find('.addressLocality').text();
  json.contract = $(this).find('.contract').text().trim();
  json.date = $(this).find('.job-details-right').text().trim();
  json.tags.push( $(this).find('.job-tags').text().trim());
  _arrayOfJobs.push(json);

  console.log(_arrayOfJobs);*/
});

  });
}
